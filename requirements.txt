# Dependencies are intentionally left unpinned so Streamlit Cloud can resolve
# versions compatible with its Python runtime. The folium maps render with an
# explicit st_folium width (see MAP_WIDTH in app_pmtiles_assessment_s3.py), so
# the app no longer depends on a specific streamlit / streamlit-folium version.
rasterio
numpy
pillow
matplotlib
folium
streamlit
streamlit-folium
boto3
botocore
pandas
altair
pmtiles
folium-pmtiles
xarray
netCDF4
h5netcdf
h5py
shapely
